/**
 * vectors.S — Exception vector table  (XN349G0, v1.11, 1994-03-10)
 *
 * The vector table is defined as a C struct in vectors.h and initialised
 * as a const object in vectors.c.  This file is deliberately kept empty;
 * the C-struct approach is used instead.
 *
 * See: src/XN349G0/vectors.c   — struct definition + initialiser
 *      src/shared/vectors.h    — struct type declaration
 */
